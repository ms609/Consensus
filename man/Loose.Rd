% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selection.R
\name{Loose}
\alias{Loose}
\title{Loose consensus tree}
\usage{
Loose(trees)
}
\arguments{
\item{trees}{A list of trees, or a \code{multiPhylo} object; all entries must
share the same leaf labels.}
}
\value{
\code{Loose()} returns the consensus tree, an object of class \code{phylo},
rooted as in the first entry of \code{trees}.
}
\description{
\code{Loose()} returns the loose consensus, also known as the semi-strict or
combinable-component consensus \insertCite{Bremer1990}{Consensus}.  It
contains every split that is contradicted by none of the input trees;
equivalently, every split that is compatible with each input tree.
}
\details{
The loose consensus is always at least as resolved as the strict consensus
(\code{\link[=Strict]{Strict()}}) and never includes a grouping that conflicts with any input
tree.  It is incomparable with the majority-rule consensus (\code{\link[=Majority]{Majority()}}):
a split present in most trees may still be contradicted by a minority, and so
excluded from the loose consensus, whereas a split occurring in a single tree
is retained if no other tree contradicts it.
}
\examples{
trees <- ape::as.phylo(0:5, 8)
Loose(trees)

}
\references{
\insertAllCited{}
}
\seealso{
Other consensus methods: \code{\link[=Strict]{Strict()}}, \code{\link[=Majority]{Majority()}}, \code{\link[=Greedy]{Greedy()}}.

Other consensus methods:
\code{\link[=Adams]{Adams()}},
\code{\link[=Average]{Average()}},
\code{\link[=Frequency]{Frequency()}},
\code{\link[=Greedy]{Greedy()}},
\code{\link[=Local]{Local()}},
\code{\link[=Majority]{Majority()}},
\code{\link[=MajorityPlus]{MajorityPlus()}},
\code{\link[=Quartet]{Quartet()}},
\code{\link[=RStar]{RStar()}},
\code{\link[=Strict]{Strict()}}
}
\concept{consensus methods}
